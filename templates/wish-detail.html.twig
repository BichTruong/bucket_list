{% extends 'base.html.twig' %}
{% block title %}{{ parent() }} | accueil{% endblock %}
{% block main %}
    <div id="gradbg">
            <h1>Wish Details</h1>
            <ul>
                <li>{{ Whish.title}} </li>
                <li>{{ Whish.description}} </li>
                <li>{{ Whish.dateCreated | date('d/m/Y h:i')}} </li>
            </ul>
    </div>
{% endblock %}